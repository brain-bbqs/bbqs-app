# Local Python toolchain for the KG validation harness — never commit it.
.venv/
__pycache__/

# The graph and generated artifacts (.ttl, .jsonld) ARE tracked as versioned outputs.
# CAUTION: commit only an anon (RLS-limited) export here. A full-access export can contain
# restricted data (PII) and MUST NOT be committed.
